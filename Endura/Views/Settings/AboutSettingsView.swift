import Foundation
import SwiftUI

struct AboutSettingsView: View {
    public var body: some View {
        VStack {
            Text("Endura v1.0.0")
                .font(.title)
                .fontWeight(.semibold)
                .frame(maxWidth: .infinity, alignment: .center)
        }
    }
}
